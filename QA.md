# 구구단 게임 검증

2026-09-22 School_Timer 연결 및 1·2·3분 도전 개편 기준.

- TypeScript + Vite build 통과(54 modules). 별도 lint/test framework 없음; 새 의존성 없음.
- `.qa/shared/database-check.sql`: 임시 PostgreSQL과 실제 School_Timer에서 서버 채점, 상위3명, 시간별 분리, 중복 제출, 재도전 강제, 잘못된 입력, 공유 누적, 기존 프로필, 조기 종료, 역할별 접근 제한 검증 통과. 실제 DB 검증은 트랜잭션 전체 롤백. 이후 신규 게임 테이블0행 확인.
- 실제 Edge API: 번호 세션 발급·프로필·순위 조회200, 인증 누락·서명 변조401. `.qa/shared/live-api.json`. 실제 학생 기록을 생성하지 않음.
- 보안 Advisor: 새 테이블의 RLS enabled/no policy INFO는 브라우저 직접 접근을 차단하는 의도된 설정. 기존 School_Timer 테이블·정책 미수정.
- 브라우저 QA는 별도127.0.0.1:5176 화면과 로컬fixture API 사용. 순위·프로필·누적 기록 캡처에 보이는 점수는 테스트 데이터이며 실제 서버로 보내지 않음.
- 1280×650,768×650,375×667,390×844,360×640,320×568,844×390에서 홈·단 선택·약점 연습·플레이·결과·성장 기록·최근 도전·명예의 전당56개 화면 확인. `.qa/shared/*-<width>x<height>.jpg`, `metrics.json`.
- 주요 조작 요소 경계 밖/문서 스크롤/13pt 미만 글자 없음. 극단적인 확대·작은 높이에서는 접근성을 위해 스크롤 허용.
- 번호23 확인 취소→재선택→확정→새로고침 유지 확인. 기본 소리OFF 및 ON→OFF 확인.
- 오답0입력 시 정답 공개 없이 같은 식 유지, 입력만 초기화. 이후2단8개 서로 다른 식을 맞혀 완료: 정답8·오답1·응답9. 콤보 문구 없이5연속 보너스 유지.
- 2분2:00·3분3:00 시작 확인. 시간별 내 기록과 명예의 전당 탭 변경 확인.
- `.qa/shared/audio-check.mjs`: 강화된 정답5/연속 정답7개 음 레이어, 지연 생성, 즉시 음소거, 숨긴 문서 무음,10 voice 상한, 종료 정리 통과. 이전 arcadescript의 음 레이어 개수는 개편 전 기대값이므로 현 구성에 맞춘 shared검증 사용.
- 실물 크롬북·휴대폰 스피커의 청음 품질과 음량, 스크린리더는 직접 검증하지 못함.

이전 `.qa/arcade`, `.qa/revision`은 과거 버전 근거이며 이번 개편 승인 근거로 사용하지 않음.

## 최종 보완 검증
- 1분 도전 실제60초 경과 후 자동 완료 확인.
- 401 세션 만료 시 같은 번호로 재발급하고 원래 요청1회 재시도; 반복401은 종료. `.qa/shared/session-check.mjs` 통과.
- 로컬 API에서 A게임 저장503을 강제로 발생시킨 뒤 B게임을 완료: A/B 두 고유 ID가 모두 저장됨. 완료 게임 큐로 화면 이동 후에도 재시도 보존.
- 실제 localhost 화면에서 School_Timer 프로필 이미지 로딩(192×192)과 빈 명예의 전당 확인. 실제 학생 최고 기록을 임의 생성하지 않음.

Vite 환경변수 변경으로 서버가 재시작되는 동안 기존 탭에 개발용 HMR WebSocket 오류1회가 남았으나, 새로고침 후 실제 API·화면·이미지 조회는 정상 확인했습니다. 의도적으로 발생시킨503 오류를 제외한 최종 QA 탐색에서 앱 런타임 오류는 없었습니다.

## 최종 독립 검토
- 기능·디자인 시스템: PASS, 차단 사항 없음. `.omo/evidence/shared-functional-final.md`.
- 시각·한글: 전체60캡처 직접 확인 후 PASS, 차단 사항 없음. `.omo/evidence/shared-visual-final.md`.
- 최종56화면 + 저장 오류1화면의57개 DOM 측정: 넘침·화면 밖 요소·13pt 미만 글자0건.320×568 홈 마스코트와 시간 선택 탭 사이 간격 보완 후 재촬영.
- 최종 빌드: `index-BJajgDRF.js`, `index-CZVzTYjD.css`. 실제 연결된 사용자 홈 유지, 테스트 브라우저·fixture서버·임시 PostgreSQL 종료.
